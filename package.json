{
    "name": "nft-pawnshop",
    "version": "0.1.0",
    "author": "Bryan Wee",
    "license": "UNLICENSED",
    "scripts": {
        "build": "./build.sh",
        "deploy": "npm run build && near deploy --wasmFile out/main.wasm --accountId $PAWNSHOP_ID && near call $PAWNSHOP_ID new --account_id $PAWNSHOP_ID",
        "dev-deploy": "npm run build && near deploy --wasmFile out/main.wasm --accountId $PAWNSHOP_ID && near call $PAWNSHOP_ID new --account_id $PAWNSHOP_ID"
    }
}
